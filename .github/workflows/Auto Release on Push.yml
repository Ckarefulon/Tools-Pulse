name: Auto Release on Push

on:
  push:
    branches:
      - main   # 仅当 push 到 main 分支时触发，避免 tag 推送形成循环

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write   # 推送 tag 及创建 Release 所需权限

    steps:
      - name: Checkout 代码
        uses: actions/checkout@v6   # 若 v6 不可用，请改为 @v4
        with:
          fetch-depth: 0            # 拉取完整历史以获取所有 tag

      - name: 获取上一版本 tag
        id: latest-tag
        run: |
          git fetch --tags
          # 按版本号降序排序，取最大的那个（如 v9, v10 等均可正确处理）
          LATEST_TAG=$(git tag --sort=-v:refname | head -n 1)
          echo "上一版本 tag: $LATEST_TAG"
          echo "tag=$LATEST_TAG" >> "$GITHUB_OUTPUT"

      - name: 计算新版本号（整数 +1）
        id: new-version
        run: |
          TAG="${{ steps.latest-tag.outputs.tag }}"
          # 去掉前缀 v，得到数字（例如 v5 → 5）
          VERSION_NUM="${TAG#v}"
          # 加一
          NEW_NUM=$((VERSION_NUM + 1))
          NEW_TAG="v${NEW_NUM}"
          echo "新版本号: $NEW_TAG"
          echo "new_tag=$NEW_TAG" >> "$GITHUB_OUTPUT"

      - name: 创建并推送新 tag
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git tag "${{ steps.new-version.outputs.new_tag }}"
          git push origin "${{ steps.new-version.outputs.new_tag }}"

      - name: 创建 GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.new-version.outputs.new_tag }}
          name: Release ${{ steps.new-version.outputs.new_tag }}
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
